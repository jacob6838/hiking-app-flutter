class MovingFilter {
  double x;
  double y;
  double theta;
  double x_dot;
  double y_dot;
  double theta_dot;

  MovingFilter(this.x, this.y, this.theta, this.x_dot, this.y_dot, this.theta_dot);

  // void iterate(x, y, theta, x_dot, y_dot, theta_dot) {
  //
  // }
}